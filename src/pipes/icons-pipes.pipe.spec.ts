import { IconsPipesPipe } from './icons-pipes.pipe';

describe('IconsPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new IconsPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
